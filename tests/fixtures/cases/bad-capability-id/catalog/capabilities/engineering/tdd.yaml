apiVersion: agent-plugins/v1
kind: Capability
metadata:
  id: tdd
spec:
  description: Drive implementation test-first.
  cardinality: one
  implementations:
    - package: acme-skills
      component: tdd
