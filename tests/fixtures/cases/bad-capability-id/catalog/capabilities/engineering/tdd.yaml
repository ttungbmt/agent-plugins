apiVersion: agent-plugins.dev/v1alpha1
kind: Capability
metadata:
  id: tdd
spec:
  description: Drive implementation test-first.
  cardinality: one
  implementations:
    - package: acme-skills
      component: tdd
