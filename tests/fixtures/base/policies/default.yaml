apiVersion: agent-plugins/v1
kind: Policy
metadata:
  id: default
spec:
  description: V1 baseline.
  componentTypes:
    skill: allow
    agent: allow
    command: allow
    hook: deny
    mcp: deny
    lsp: deny
