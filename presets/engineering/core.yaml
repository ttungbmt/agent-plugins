apiVersion: agent-plugins.dev/v1alpha1
kind: Preset
metadata:
  id: engineering/core
spec:
  description: Baseline engineering loop — model, build test-first, review.
  capabilities:
    - engineering.domain-modeling
    - engineering.testing.tdd
    - engineering.review
