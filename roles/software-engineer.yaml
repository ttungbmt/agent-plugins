apiVersion: agent-plugins.dev/v1alpha1
kind: Role
metadata:
  id: software-engineer
spec:
  description: Reusable role baseline for someone writing production software.
  # A Role reaches Capabilities through Presets. domain-model.md section 15
  presets:
    - workflow/core
    - engineering/core
