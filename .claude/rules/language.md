# Language

Talk to the user in Vietnamese. Technical docs under `docs/` are written in Vietnamese prose that keeps the English
glossary terms from `CONTEXT.md`. Everything else — code, comments, JSDoc, specs, tickets, commit messages,
`CLAUDE.md` and rules — is written in English.

Pick the language by where the file lives, not by the language of the request.
