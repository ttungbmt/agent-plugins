apiVersion: agent-plugins.dev/v1alpha1
kind: Capability
metadata:
  id: engineering.review
spec:
  description: Review a diff against standards and spec before committing.
  cardinality: one
  implementations:
    - package: mattpocock-skills
      component: code-review
